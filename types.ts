export interface BusLine {
  id: string;
  lineNumber: string;
  destination: string;
  companyName: string;
  companyLogoText: string;
  color: string; // Hex code for the bus body
  year: string;
  description: string;
  history: string;
  plate: string;
}
